class SignupService {
    constructor($window, $state, $http) {
      this.$window = $window;
      this.$state = $state;
      this.$http = $http;
    }
    signin() {
      
    }
}

export default SignupService;